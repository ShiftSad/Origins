name: Origins
version: '${version}'
main: codes.shiftmc.origins.Origins
loader: codes.shiftmc.origins.OriginsLoader
description: Paper Test Plugin
api-version: '1.20.6'
folia-supported: true
dependencies:
  server:
    CommandAPI:
      load: BEFORE
      required: true
      join-classpath: true
